DJANGO_SECRET_KEY=replace-with-a-long-random-string
DJANGO_DEBUG=1
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost

# Optional override for demo bootstrap password (defaults to change-me-before-use)
# QAID_DEFAULT_ADMIN_PASSWORD=change-me-before-use
